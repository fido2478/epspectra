#include "SINK.h"
#include <interface.h>  /* C++ class header */
#include <string.h>

#include <datatype.h>

/*---------------------------------------------
 * Describe Procedure & Function
 * This is SINK_data.c
 *---------------------------------------------*/

/******** Define Function ******************/
/*
void CREATE_SINK()
{
 InterCreateSink();
}

char* GETNAMESINK()
{
 return TheNameOfSink();
}

StrlSampleSize GET_SINK_MAXOUTPUTSIZE()
{
  unsigned long sz=InterSinkgetMaxOutputSize();
  StrlSampleSize size=uld2str(sz);
  printf("[2]GETMAXOUTPUTSIZE => %lu = %s(strl)\n",sz,size);
  return size;
}

StrlSampleRange GET_SINK_RANGE(StrlSampleIndex i, StrlSampleSize s)
{
  StrlSampleRange str=(StrlSampleRange)malloc(sizeof(StrlSampleRange));
  strcpy(str,i);
  strcat(str,s);
  printf("[3]GETSAMPLERANGE [%s, %s]= [%s](strl)\n",i,s,str);
  return str;

}
*/
/*
void SINK_MARK_DATA(StrlSampleRange x)
{
  InterSinkmarkdata(str2SRange(x));
}
*/
